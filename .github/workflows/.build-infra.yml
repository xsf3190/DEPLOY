name: Build Infra
on: 
  workflow_dispatch:
  repository_dispatch:
    types: trigger_build_infra
permissions:
  contents: write
env:
  TF_VAR_FROM_EMAIL: ${{ github.event.client_payload.from_email }}
  TF_VAR_PUBLIC_KEY: ${{ github.event.client_payload.public_key }}
  # TF_API_TOKEN: ${{ github.event.client_payload.terraform_token }}
  TF_API_TOKEN: "SlK9GJfqIHc6uQ.atlasv1.2HuI23zuoEmzKoUCAusIhb2RbXreYRu3FfGfgViW7Hw41scKuXGtL0rJllrxdsbuvfc"
jobs:
  build_page:
    name: Build Infra
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./terraform_lambda_api
    steps:
      # Get latest updates repo
      - uses: actions/checkout@v3

      - uses: hashicorp/setup-terraform@v2
        with:
          cli_config_credentials_token: ${TF_API_TOKEN}
      
      - run: terraform init
      - run: terraform plan
      - run: terraform apply
